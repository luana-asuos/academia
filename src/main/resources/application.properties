# configurando nosso projeto

# Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 1 - obrigatorio: username
spring.datasource.username=root

# 2 - obrigatorio: senha
spring.datasource.password=Vida4125$

# 3 - obrigatorio: url do banco protocolo://servidor:porta/nome_da_base
#spring.datasourse.url =jdbc:mysql://localhost/db_aluno?createDatabaseIfNotExist=true
spring.datasource.url=jdbc:mysql://localhost/Db_academiaplaygym?createDatabaseIfNotExist=true

# 4 - obrigatorio: dialeto (sotaque) do SQL do banco
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# 5 - opcional: mostrar no console as instruções SQL geradas
spring.jpa.show_sql=true

spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=create-drop
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true
